# Transparent Controller Kernel MVP

Standard library only. No requests. No SDK. Provider swaps happen through provider_config.json, not code.

Run:

```powershell
cd release
py .\run_with_local_secrets.py
```

First run creates local_secrets.json. Edit it locally. Do not upload or commit it.
